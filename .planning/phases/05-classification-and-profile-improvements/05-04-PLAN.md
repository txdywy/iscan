---
phase: 05-classification-and-profile-improvements
plan: "04"
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - internal/model/model.go
  - internal/profile/profile.go
  - internal/profile/correlate.go
  - internal/profile/profile_test.go
  - internal/scanner/scanner.go
autonomous: true
requirements:
  - F-09
  - F-17
must_haves:
  truths:
    - "When control targets exist, profile health excludes them from diagnostic aggregation and evaluates diagnostics against a control baseline."
    - "When no control targets exist, profile falls back to the current all-target aggregation behavior instead of inventing a synthetic baseline."
    - "Cross-target correlation runs after scan assembly and appends report-level findings before CLI analysis/report rendering consume the scan."
  artifacts:
    - path: internal/profile/profile.go
      provides: control-vs-diagnostic partition and baseline-aware profile scoring
      contains: "BuildProfile"
    - path: internal/profile/correlate.go
      provides: report-level control/diagnostic correlation helper
      contains: "Correlate"
    - path: internal/scanner/scanner.go
      provides: post-assembly correlation invocation before report return
      contains: "profile.Correlate"
    - path: internal/profile/profile_test.go
      provides: fallback and correlation regression tests
      contains: "TestBuildProfile"
    - path: internal/model/model.go
      provides: additive finding types needed for correlation outputs
      contains: "Finding"
  key_links:
    - from: internal/scanner/scanner.go
      to: internal/profile/correlate.go
      via: report-level correlation pass after per-target classification
      pattern: "Correlate"
    - from: internal/profile/profile.go
      to: internal/model/model.go
      via: `Target.Control` partitioning
      pattern: "Control"
---

<objective>
Split control and diagnostic profile behavior, then add a report-level correlation pass after scan assembly.

Purpose: Deliver accurate baseline-aware health metrics and multi-target findings without moving per-target detection into probes or scanner internals.
Output: Baseline-aware profile aggregation, additive correlation finding types if needed, and scanner wiring that appends control-vs-diagnostic findings before returning the scan report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md
@.planning/phases/05-classification-and-profile-improvements/05-RESEARCH.md
@internal/model/model.go
@internal/profile/profile.go
@internal/profile/profile_test.go
@internal/scanner/scanner.go
@internal/targets/targets.go
@cmd/iscan/main.go

<interfaces>
From internal/profile/profile.go:
```go
func BuildProfile(report model.ScanReport) Profile
```

From internal/scanner/scanner.go:
```go
func Run(ctx context.Context, options model.ScanOptions) model.ScanReport
```

From internal/model/model.go:
```go
type Target struct {
    Name    string
    Domain  string
    Control bool
    QUICPort int
}

type ScanReport struct {
    Targets  []TargetResult
    Findings []Finding
    Warnings []string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add target partition helpers and baseline-aware profile aggregation</name>
  <objective>Implement D-07 through D-09 in `internal/profile` so controls stop polluting diagnostic health while no-control scans still work.</objective>
  <files>internal/profile/profile.go, internal/profile/profile_test.go, internal/model/model.go</files>
  <read_first>internal/profile/profile.go, internal/profile/profile_test.go, internal/model/model.go, internal/targets/targets.go, .planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md</read_first>
  <behavior>
    - Control targets are identified only through `model.Target.Control`.
    - With controls present, diagnostic health metrics exclude control observations from success-rate aggregation.
    - With no controls present, profile behavior matches the current all-target aggregation path.
  </behavior>
  <behavior>
    - Control targets are identified only through `model.Target.Control`.
    - With controls present, profile computes a control baseline first and then evaluates diagnostic health against that baseline rather than averaging controls into diagnostics.
    - With no controls present, profile behavior matches the current all-target aggregation path.
  </behavior>
  <action>Refactor `internal/profile/profile.go` to partition `report.Targets` into control and diagnostic groups per D-07, compute baseline health from controls when they exist, and compare diagnostic health against that baseline per D-08 instead of merely excluding controls from the denominator. Keep `recommend.Rank` compatibility by preserving existing `Profile` fields; only add new fields in `internal/model/model.go` if a correlation finding type needs a new enum constant. If you touch DNS aggregation, reuse a slice-aware DNS collection path so `[]model.DNSObservation` data is not dropped.</action>
  <verify>
    <automated>go test ./internal/profile -run 'TestBuildProfile(ComputesTiers|UsesControlTargetsAsBaseline|ComparesDiagnosticsAgainstControlBaseline|FallsBackToAllTargetsWhenNoControlsExist)' -count=1</automated>
  </verify>
  <acceptance_criteria>Profile health computes a control baseline when control targets exist, compares diagnostic health against that baseline, preserves current behavior when no controls exist, and keeps the existing top-level `Profile` fields intact for recommendation/report consumers.</acceptance_criteria>
  <done>F-09 is implemented with exact regression coverage for control-baseline exclusion and fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a report-level control-vs-diagnostic correlation helper</name>
  <objective>Implement D-10 and D-11 as a post-scan correlation pass rather than a per-target detector or profile-only side effect.</objective>
  <files>internal/profile/correlate.go, internal/profile/profile_test.go, internal/model/model.go</files>
  <read_first>internal/profile/profile.go, internal/model/model.go, internal/profile/profile_test.go, .planning/phases/05-classification-and-profile-improvements/05-RESEARCH.md</read_first>
  <behavior>
    - Correlation inspects all targets after per-target findings already exist.
    - Findings can describe layer-specific divergence with `Finding.Layer` set to DNS/TCP/TLS/QUIC as appropriate.
    - Correlation does not depend on `--analyze`.
  </behavior>
  <action>Create `internal/profile/correlate.go` with an exported `Correlate(report model.ScanReport) []model.Finding` helper so external-package tests can call it directly and `scanner.Run` can append its outputs before returning. Compare control success against diagnostic failure by layer per D-10/D-11, and if a new finding type is needed add it additively in `internal/model/model.go` without changing existing enums or report formatting. Assign correlation findings using the shared confidence calibrator or an equivalent profile-side helper so report-level findings rise only when control-vs-diagnostic corroboration is real. Keep correlation findings conservative: require at least one control success and at least one diagnostic failure on the same layer before emitting a divergence conclusion.</action>
  <verify>
    <automated>go test ./internal/profile -run 'TestCorrelate(ControlSuccessVsDiagnosticFailure|UsesEvidenceWeightedConfidence|DoesNotEmitWithoutControlBaseline)' -count=1</automated>
  </verify>
  <acceptance_criteria>A dedicated correlation helper emits report-level findings from full-report evidence, applies evidence-weighted confidence, and can be unit-tested independently of CLI `--analyze` mode.</acceptance_criteria>
  <done>Cross-target correlation is implemented as a profile-adjacent helper, not inside the per-target classifier loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire correlation into scanner report assembly</name>
  <objective>Ensure ordinary scan results include correlation findings before report/profile/recommend consumers run.</objective>
  <files>internal/scanner/scanner.go, internal/profile/correlate.go</files>
  <read_first>internal/scanner/scanner.go, internal/profile/correlate.go, cmd/iscan/main.go</read_first>
  <behavior>
    - Per-target `classifier.Classify` still runs exactly once per target.
    - Report-level correlation happens only after `report.Targets` and flattened findings are assembled.
    - CLI analysis remains additive; no report rendering redesign is introduced.
  </behavior>
  <action>Update `internal/scanner/scanner.go` to append `profile.Correlate(report)` results after `report.Targets` and per-target findings have been assembled, and before `report.Duration` is finalized and returned. Keep the existing probe/classifier/report seams intact, do not move per-target detection into scanner, and do not modify recommendation or terminal rendering logic beyond the compatibility guaranteed by shared `model.ScanReport` structures.</action>
  <verify>
    <automated>go test ./internal/profile ./internal/scanner -count=1</automated>
  </verify>
  <acceptance_criteria>Ordinary `scanner.Run` output contains correlation findings without requiring `--analyze`, and scanner still assembles per-target results the same way before the new second pass runs.</acceptance_criteria>
  <done>F-17 is wired into the scan pipeline at the report-assembly seam mandated by D-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report.Targets -> profile baseline logic | Multi-target results are partitioned into control and diagnostic evidence and can be mis-scored if the partition is wrong. |
| assembled ScanReport -> correlation pass | Higher-order findings are derived from already-classified target results and must not depend on CLI-only analysis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | T | profile baseline scoring | mitigate | Partition targets only by `model.Target.Control` per D-07 and assert exact fallback/control-baseline behavior in `internal/profile/profile_test.go`. |
| T-05-08 | T | report-level correlation | mitigate | Emit divergence findings only when a control success and comparable diagnostic failure coexist on the same layer, and run the helper from `scanner.Run` before `--analyze` gating can hide it. |
</threat_model>

<verification>
- `go test ./internal/profile ./internal/scanner -count=1`
</verification>

<success_criteria>
- Control targets no longer inflate diagnostic profile health when they exist.
- No-control scans keep the current all-target aggregation behavior.
- A report-level correlation helper exists and is invoked from `scanner.Run` before the scan report is returned.
- Recommendation/report compatibility is preserved because existing `Profile` fields and `ScanReport` flow remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-classification-and-profile-improvements/05-04-SUMMARY.md`
</output>
