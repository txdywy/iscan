---
phase: 05-classification-and-profile-improvements
plan: "05"
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - internal/classifier/classifier.go
  - internal/classifier/classifier_test.go
autonomous: true
requirements:
  - F-17
must_haves:
  truths:
    - "A conservative TLS/QUIC divergence detector emits a finding only when comparable evidence shows TLS success and QUIC failure for the same target intent."
    - "Targets with `QUICPort == 0`, missing QUIC observations, permission errors, or obvious unsupported-transport errors do not trigger divergence findings."
    - "Divergence logic stays inside `internal/classifier` as a per-target detector; cross-target correlation remains in the report-level pass from 05-04."
  artifacts:
    - path: internal/classifier/classifier.go
      provides: TLS/QUIC divergence detector with false-positive guards
      contains: "QUIC"
    - path: internal/classifier/classifier_test.go
      provides: positive and negative divergence regression tests
      contains: "Divergence"
  key_links:
    - from: internal/classifier/classifier.go
      to: internal/model/model.go
      via: target QUIC intent plus TLS/QUIC observation comparison
      pattern: "QUICPort|TLSObservation|QUICObservation"
---

<objective>
Add a conservative TLS/QUIC divergence detector and lock it down with regression tests that guard against false positives.

Purpose: Surface likely UDP/QUIC interference without confusing disabled or unsupported QUIC with censorship evidence.
Output: A per-target detector that emits a dedicated divergence finding only when the target actually intended QUIC and the evidence is comparable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md
@.planning/phases/05-classification-and-profile-improvements/05-RESEARCH.md
@internal/classifier/classifier.go
@internal/classifier/classifier_test.go
@internal/model/model.go
@internal/targets/targets.go

<interfaces>
From internal/model/model.go:
```go
type Target struct {
    Name     string
    Domain   string
    Control  bool
    QUICPort int
}

type TLSObservation struct {
    Address string
    SNI     string
    Success bool
    Error   string
}

type QUICObservation struct {
    Address string
    SNI     string
    Success bool
    Error   string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the conservative TLS/QUIC divergence detector</name>
  <objective>Add the D-12/D-13 detector without moving cross-target logic back into probes or scanner.</objective>
  <files>internal/classifier/classifier.go, internal/classifier/classifier_test.go</files>
  <read_first>internal/classifier/classifier.go, internal/classifier/classifier_test.go, internal/model/model.go, internal/targets/targets.go, .planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md</read_first>
  <behavior>
    - The detector requires at least one successful TLS observation and at least one failed QUIC observation for the same target intent.
    - The detector ignores targets whose `QUICPort` is `0`.
    - The detector ignores absent QUIC results and obvious unsupported/permission-style failures.
  </behavior>
  <action>Add a new per-target detector in `internal/classifier/classifier.go` that compares normalized TLS and QUIC evidence for the same `model.TargetResult` per D-12. Gate it conservatively per D-13: require explicit QUIC intent (`result.Target.QUICPort > 0`), at least one TLS success, at least one QUIC failure, and comparable SNI/target-family evidence before emitting a divergence finding that suggests UDP/QUIC filtering. Do not treat missing QUIC observations, disabled QUIC, permission errors, or unsupported-transport errors as censorship evidence. Keep this logic in `internal/classifier`; cross-target correlation remains the 05-04 report-level pass.</action>
  <verify>
    <automated>go test ./internal/classifier -run 'TestClassify(ReportsTLSQUICDivergenceWhenTLSSucceedsAndQUICFails|NoTLSQUICDivergenceForDisabledQUIC)' -count=1</automated>
  </verify>
  <acceptance_criteria>The detector emits one dedicated divergence finding only for comparable TLS-success/QUIC-failure evidence on QUIC-capable targets.</acceptance_criteria>
  <done>TLS/QUIC divergence is available as a conservative per-target classifier signal without changing scanner or probe boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests for false-positive guards</name>
  <objective>Prove the divergence detector stays conservative in the exact scenarios called out by the phase context.</objective>
  <files>internal/classifier/classifier_test.go</files>
  <read_first>internal/classifier/classifier_test.go, internal/classifier/classifier.go, internal/targets/targets.go</read_first>
  <behavior>
    - Positive case: TLS success + QUIC failure on a QUIC-capable target produces the divergence finding.
    - Negative case: `QUICPort == 0` does not produce the divergence finding.
    - Negative case: unsupported/permission/missing-support style QUIC errors do not produce the divergence finding.
    - Negative case: no successful TLS observation means no divergence finding.
  </behavior>
  <action>Extend `internal/classifier/classifier_test.go` with exact assertions for the positive divergence case and the false-positive guards required by the user: disabled QUIC (`Target.QUICPort == 0`), missing QUIC support, permission-like errors, and no successful TLS comparison baseline. Reuse inline fixtures from `internal/targets/targets.go` semantics instead of inventing a second control-flag or QUIC-intent source.</action>
  <verify>
    <automated>go test ./internal/classifier -count=1</automated>
  </verify>
  <acceptance_criteria>The classifier suite explicitly proves the divergence detector fires only on comparable evidence and stays silent for disabled/missing-support scenarios.</acceptance_criteria>
  <done>The false-positive guards for Phase 5’s QUIC/TLS divergence requirement are regression-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target metadata -> divergence detector | QUIC intent comes from `Target.QUICPort` and must be honored to avoid false positives. |
| TLS/QUIC observations -> divergence finding | Transport-specific errors can be misread as censorship unless compared conservatively. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | T | TLS/QUIC divergence detector | mitigate | Require QUIC intent, TLS success, QUIC failure, and comparable evidence before emitting a divergence finding, per D-12/D-13. |
| T-05-10 | T | false-positive guard rails | mitigate | Add explicit regression tests for `QUICPort == 0`, missing QUIC observations, permission errors, and unsupported-transport errors so those cases cannot be misclassified as UDP filtering. |
</threat_model>

<verification>
- `go test ./internal/classifier -count=1`
</verification>

<success_criteria>
- A dedicated TLS/QUIC divergence detector exists inside `internal/classifier`.
- The detector emits findings only for comparable TLS-success/QUIC-failure evidence.
- Disabled QUIC, missing-support, and permission-error scenarios are explicitly covered by regression tests and do not produce divergence findings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-classification-and-profile-improvements/05-05-SUMMARY.md`
</output>
