---
phase: 05-classification-and-profile-improvements
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - internal/classifier/classifier.go
  - internal/classifier/classifier_test.go
  - internal/classifier/confidence.go
autonomous: true
requirements:
  - F-15
must_haves:
  truths:
    - "Confidence still uses `model.ConfidenceLow|Medium|High`, but promotions now come from corroboration signals instead of detector-local literals."
    - "Single isolated failures stay low confidence; repeated or cross-layer corroborated evidence can promote to medium or high per D-04 through D-06."
    - "Phase 4 RCODE semantics remain preserved while other detector outputs become evidence-weighted."
  artifacts:
    - path: internal/classifier/confidence.go
      provides: shared confidence calibration helper
      contains: "Confidence"
    - path: internal/classifier/classifier.go
      provides: detector wiring through the shared calibrator
      contains: "Confidence"
    - path: internal/classifier/classifier_test.go
      provides: promotion and non-promotion regression tests
      contains: "Confidence"
  key_links:
    - from: internal/classifier/classifier.go
      to: internal/classifier/confidence.go
      via: detector confidence selection
      pattern: "ConfidenceLow|ConfidenceMedium|ConfidenceHigh"
---

<objective>
Add a shared confidence calibration helper and route detector confidence through evidence-weighted promotion rules.

Purpose: Replace ad hoc confidence assignment with one conservative calibrator while preserving the existing enum and report compatibility.
Output: A reusable confidence helper plus detector updates and regression tests that prove when confidence should and should not rise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md
@.planning/phases/05-classification-and-profile-improvements/05-RESEARCH.md
@internal/classifier/classifier.go
@internal/classifier/classifier_test.go
@internal/model/model.go

<interfaces>
From internal/model/model.go:
```go
type Confidence string

const (
    ConfidenceLow    Confidence = "low"
    ConfidenceMedium Confidence = "medium"
    ConfidenceHigh   Confidence = "high"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce a shared classifier confidence calibrator</name>
  <objective>Create one helper for evidence-weighted confidence mapping per D-04 and D-05 so detector bodies stop owning separate promotion math.</objective>
  <files>internal/classifier/confidence.go, internal/classifier/classifier.go, internal/classifier/classifier_test.go</files>
  <read_first>internal/classifier/classifier.go, internal/classifier/classifier_test.go, internal/model/model.go, .planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md</read_first>
  <behavior>
    - Output stays in `model.Confidence` values only.
    - The helper can evaluate evidence count, resolver count, control/diagnostic corroboration hooks, and cross-layer agreement flags.
    - No detector introduces a new numeric confidence type.
  </behavior>
  <action>Create `internal/classifier/confidence.go` only because this logic is shared across multiple detectors. Define a compact helper such as `ConfidenceSignals` + `CalibrateConfidence(...) model.Confidence` that starts from a detector-supplied base level and promotes only on real corroboration per D-04/D-06. Keep the helper package-private, keep the enum unchanged per D-05, and document the minimum promotion thresholds in code comments so later profile correlation can reuse the same promotion vocabulary.</action>
  <verify>
    <automated>go test ./internal/classifier -run 'TestClassify(KeepsSingleTLSFailureLowConfidence|ReportsRCODE_NXDOMAIN|ReportsRCODE_SERVFAIL|ReportsRCODE_REFUSED)' -count=1</automated>
  </verify>
  <acceptance_criteria>A dedicated helper file owns confidence calibration, still returns only low/medium/high, and preserves the current RCODE confidence outcomes.</acceptance_criteria>
  <done>Detector code has one shared place to ask for confidence instead of embedding promotion thresholds inline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply evidence-weighted promotions and add exact confidence assertions</name>
  <objective>Wire the calibrator into detector outputs and prove the promotion boundaries with explicit classifier tests.</objective>
  <files>internal/classifier/classifier.go, internal/classifier/classifier_test.go, internal/classifier/confidence.go</files>
  <read_first>internal/classifier/confidence.go, internal/classifier/classifier.go, internal/classifier/classifier_test.go, .planning/phases/05-classification-and-profile-improvements/05-RESEARCH.md</read_first>
  <behavior>
    - A single TLS/TCP/HTTP/QUIC failure stays low confidence.
    - Repeated corroborated evidence within a target can promote to medium.
    - High confidence requires strong corroboration, not just multiple copies of the same weak signal.
  </behavior>
  <action>Update detector implementations to call the shared calibrator instead of hardcoding confidence, except where D-05 requires preserving the existing enum contract and Phase 4 semantics. Add exact assertions in `internal/classifier/classifier_test.go` for at least: one isolated failure stays `model.ConfidenceLow`; corroborated multi-observation evidence promotes above low; and RCODE findings keep NXDOMAIN=high, SERVFAIL=medium, REFUSED=high. Use observable `classifier.Classify` outputs only.</action>
  <verify>
    <automated>go test ./internal/classifier -count=1</automated>
  </verify>
  <acceptance_criteria>The classifier suite contains explicit promotion and non-promotion assertions, and detector confidence no longer relies on scattered literals for the same corroboration rule.</acceptance_criteria>
  <done>Confidence is evidence-weighted, conservative, and still fully compatible with existing rendering/report code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector evidence -> confidence helper | Raw corroboration signals must be interpreted consistently or findings will be overstated. |
| confidence helper -> report consumers | Output must stay in the existing enum so downstream JSON/report/recommend code remains compatible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | T | confidence calibration | mitigate | Centralize promotion logic in one helper and require explicit tests that isolated failures stay low while only corroborated evidence promotes. |
| T-05-06 | T | downstream compatibility | mitigate | Preserve `model.Confidence` as the only output type per D-05 and keep Phase 4 RCODE confidence assertions passing unchanged. |
</threat_model>

<verification>
- `go test ./internal/classifier -count=1`
</verification>

<success_criteria>
- A shared classifier confidence helper exists and is the preferred path for detector confidence decisions.
- Single failures remain low confidence unless corroborated evidence justifies promotion.
- Existing enum values and Phase 4 RCODE confidence semantics are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-classification-and-profile-improvements/05-03-SUMMARY.md`
</output>
