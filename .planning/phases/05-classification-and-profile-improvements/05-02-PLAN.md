---
phase: 05-classification-and-profile-improvements
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - internal/classifier/classifier.go
  - internal/classifier/classifier_test.go
autonomous: true
requirements:
  - F-16
  - F-15
must_haves:
  truths:
    - "Each current heuristic runs as its own registered detector rather than as inline branches inside `Classify()`."
    - "The public `Classify(result)` API and existing finding types remain stable while detector bodies are extracted."
    - "Detector responsibilities map one-to-one to D-03 concerns: DNS, TCP, TLS, HTTP, QUIC, trace, SNI, transparent proxy, and RCODE logic."
  artifacts:
    - path: internal/classifier/classifier.go
      provides: registered detector implementations for existing heuristics
      contains: "Detect"
    - path: internal/classifier/classifier_test.go
      provides: regression coverage for extracted detector behavior
      contains: "TestClassify"
  key_links:
    - from: internal/classifier/classifier.go
      to: internal/classifier/classifier_test.go
      via: extracted detector behavior remains observable through Classify
      pattern: "FindingDNS|FindingTCP|FindingTLS|FindingHTTP|FindingQUIC|FindingPath"
---

<objective>
Extract the current classifier heuristics into registered detectors while preserving the public `Classify(result)` entrypoint and observable behavior.

Purpose: Finish the structural split from monolith to composable detectors without widening the API surface.
Output: All current finding logic lives behind detector implementations registered inside `internal/classifier`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md
@.planning/phases/05-classification-and-profile-improvements/05-RESEARCH.md
@internal/classifier/classifier.go
@internal/classifier/classifier_test.go
@internal/model/model.go

<interfaces>
From internal/classifier/classifier.go:
```go
func Classify(result model.TargetResult) []model.Finding
func dnsRcodeFindings(observations []model.DNSObservation, now time.Time) []model.Finding
func detectTransparentDNSProxy(observations []model.DNSObservation, now time.Time) []model.Finding
func aggregateFailures[T any](observations []T, keyFn func(T) string, successFn func(T) bool, msgFn func(T) string) []string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract DNS-facing heuristics into registered detectors</name>
  <objective>Move DNS inconsistency, suspicious answer, RCODE, and transparent proxy logic into detector implementations per D-03 without changing their outward behavior.</objective>
  <files>internal/classifier/classifier.go, internal/classifier/classifier_test.go</files>
  <read_first>internal/classifier/classifier.go, internal/classifier/classifier_test.go, .planning/phases/04-dns-enhancements/04-04-SUMMARY.md</read_first>
  <behavior>
    - DNS inconsistency still ignores A vs AAAA comparisons and ordinary overlapping GeoDNS answers.
    - RCODE findings keep Phase 4 semantics.
    - Transparent proxy logic still activates only for whoami observations.
  </behavior>
  <action>Replace the inline DNS branches in `Classify` with registered detector implementations that consume normalized evidence and return findings for DNS inconsistency, suspicious answers, per-RCODE findings, and transparent DNS proxy signals. Keep these detectors in `internal/classifier` per D-01/D-03, keep evidence strings stable unless a test asserts a clearer equivalent string, and do not add report or scanner coupling.</action>
  <verify>
    <automated>go test ./internal/classifier -run 'TestClassify(ReportsDNSInconsistencyWithoutPoisoning|DoesNotCompareAAndAAAAAsInconsistent|ReportsRCODE_NXDOMAIN|DetectsTransparentDNSProxy|NoTransparentProxyForKnownResolverIP)' -count=1</automated>
  </verify>
  <acceptance_criteria>Every DNS-related finding path now flows through registered detector code, and the existing DNS regression suite still passes unchanged.</acceptance_criteria>
  <done>DNS heuristics are no longer embedded as ad hoc branches directly inside `Classify`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract transport and path heuristics into registered detectors</name>
  <objective>Turn TCP, TLS, SNI, HTTP, QUIC, and trace logic into detector implementations while keeping the same call-site seam.</objective>
  <files>internal/classifier/classifier.go, internal/classifier/classifier_test.go</files>
  <read_first>internal/classifier/classifier.go, internal/classifier/classifier_test.go, internal/model/model.go</read_first>
  <behavior>
    - Aggregate transport failures still emit one finding per concern.
    - Trace permission errors still do not become path-quality findings.
    - SNI-correlated failures still require mixed success/failure on the same address.
  </behavior>
  <action>Refactor the remaining inline heuristics into registered detectors for TCP connection failures, TLS handshake failures, SNI-correlated failures, HTTP failures, QUIC failures, and trace path quality. Preserve `aggregateFailures`/`sniCorrelatedFailures` style helpers where they already match codebase patterns, but make detector ownership explicit so each finding type has a single implementation entry. Do not move per-target detection into `internal/scanner` or any probe package.</action>
  <verify>
    <automated>go test ./internal/classifier -run 'TestClassify(KeepsSingleTLSFailureLowConfidence|ReportsSNICorrelatedFailureWhenSameAddressDiffersBySNI|ReportsHTTPApplicationFailure|ReportsQUICHandshakeFailure|DoesNotTreatTracePermissionErrorAsPathQuality)' -count=1</automated>
  </verify>
  <acceptance_criteria>All non-DNS heuristics execute through registered detectors and still emit the current finding types through `Classify(result)`.</acceptance_criteria>
  <done>The public API is unchanged, but the monolithic heuristic body has been decomposed into concern-specific detectors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage for extracted detector sequencing and unchanged output</name>
  <objective>Prove that detector extraction did not change observable behavior beyond the planned registry refactor.</objective>
  <files>internal/classifier/classifier_test.go</files>
  <read_first>internal/classifier/classifier_test.go, internal/classifier/classifier.go</read_first>
  <behavior>
    - A mixed-result target still emits the same set of findings after extraction.
    - The deterministic order introduced in 05-01 remains intact after detectors gain real bodies.
  </behavior>
  <action>Expand `internal/classifier/classifier_test.go` with a composite regression case that exercises at least one DNS detector and one transport detector in the same `TargetResult`, then asserts the exact ordered finding types returned by `classifier.Classify`. Use this test as the executable proof that the extracted detectors preserve the public `Classify` contract per D-02.</action>
  <verify>
    <automated>go test ./internal/classifier -count=1</automated>
  </verify>
  <acceptance_criteria>The test suite proves extracted detectors still emit the same ordered finding set through the unchanged public API.</acceptance_criteria>
  <done>Phase 5 can now change confidence behavior independently of detector extraction because detector ownership is explicit and regression-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized evidence -> detector implementations | Each detector consumes already-parsed observations and must not reinterpret raw payloads inconsistently. |
| detector implementations -> finding set | Extraction could silently drop or reorder findings if coverage is incomplete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | detector extraction | mitigate | Preserve one detector per D-03 concern and run focused regression tests for every existing classifier behavior before removing inline branches. |
| T-05-04 | T | finding ordering | mitigate | Keep the registry ordered and assert exact ordered finding types in a composite external-package test so reordering is caught immediately. |
</threat_model>

<verification>
- `go test ./internal/classifier -count=1`
</verification>

<success_criteria>
- All existing classifier heuristics live in registered detector implementations.
- `Classify(result)` remains the only public entrypoint and continues to return the existing finding types.
- DNS and transport detector behavior is locked by package tests with ordered output assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-classification-and-profile-improvements/05-02-SUMMARY.md`
</output>
