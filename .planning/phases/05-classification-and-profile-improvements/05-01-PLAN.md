---
phase: 05-classification-and-profile-improvements
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/classifier/classifier.go
  - internal/classifier/classifier_test.go
autonomous: true
requirements:
  - F-16
must_haves:
  truths:
    - "`classifier.Classify(result)` remains the public entrypoint while detector execution becomes registry-driven."
    - "DNS, TCP, TLS, HTTP, QUIC, and trace observations are normalized once from `TargetResult.Results` before detectors consume them."
    - "Detector evaluation order is deterministic and local to `internal/classifier`."
  artifacts:
    - path: internal/classifier/classifier.go
      provides: package-private detector registry and normalized evidence view
      contains: "type Detector interface"
    - path: internal/classifier/classifier_test.go
      provides: regression coverage for registry ordering and DNS slice normalization
      contains: "TestClassify"
  key_links:
    - from: internal/classifier/classifier.go
      to: internal/model/model.go
      via: normalized ProbeResult.Data extraction
      pattern: "model\\.ProbeResult|model\\.DNSObservation|model\\.TargetResult"
    - from: internal/classifier/classifier.go
      to: internal/classifier/classifier_test.go
      via: deterministic Classify(result) output order
      pattern: "Classify\\("
---

<objective>
Stand up the detector registry skeleton and a normalized per-target evidence view inside `internal/classifier`.

Purpose: Create the internal contracts Phase 5 needs without changing scanner/report call sites.
Output: `Classify(result)` still works, but the classifier package now has detector orchestration hooks and one evidence-normalization path for later detector extraction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md
@.planning/phases/05-classification-and-profile-improvements/05-RESEARCH.md
@internal/classifier/classifier.go
@internal/classifier/classifier_test.go
@internal/model/model.go

<interfaces>
From internal/classifier/classifier.go:
```go
func Classify(result model.TargetResult) []model.Finding
func collectObservations[T any](results []model.ProbeResult, layer model.Layer) []T
func collectObservation[T any](results []model.ProbeResult, layer model.Layer) *T
func collectAllDNSObservations(results []model.ProbeResult) []model.DNSObservation
```

From internal/model/model.go:
```go
type TargetResult struct {
    Target   Target
    Error    string
    Results  []ProbeResult
    Findings []Finding
}

type ProbeResult struct {
    Layer Layer
    Data  any
}

type Finding struct {
    Type       FindingType
    Layer      Layer
    Confidence Confidence
    Evidence   []string
    ObservedAt time.Time
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce classifier-local detector contracts and normalized evidence</name>
  <objective>Create the package-private registry and one normalized evidence builder per D-01 and D-02 so later plans can add detectors without rescanning raw `[]model.ProbeResult`.</objective>
  <files>internal/classifier/classifier.go, internal/classifier/classifier_test.go</files>
  <read_first>internal/classifier/classifier.go, internal/classifier/classifier_test.go, internal/model/model.go, .planning/phases/05-classification-and-profile-improvements/05-CONTEXT.md, .planning/phases/05-classification-and-profile-improvements/05-RESEARCH.md</read_first>
  <behavior>
    - `Classify(result model.TargetResult) []model.Finding` stays unchanged.
    - Single `model.DNSObservation` and `[]model.DNSObservation` payloads normalize into one DNS evidence slice.
    - Detector execution order is stable across runs.
  </behavior>
  <action>Refactor `internal/classifier/classifier.go` to define a package-private `Detector` interface or equivalent function adapter, an ordered registry populated inside `internal/classifier` per D-01/D-02, and a `targetEvidence`/equivalent normalized view that collects DNS/TCP/TLS/HTTP/QUIC/trace observations once from `result.Results`. Keep all registry state inside `internal/classifier`, keep `Classify(result)` as the only public entrypoint, and do not move any detection into scanner/profile/report. Preserve the existing `collectAllDNSObservations` slice-handling behavior from Phase 4 in the new normalization path.</action>
  <verify>
    <automated>go test ./internal/classifier -run 'TestClassify(ReportsDNSInconsistencyWithoutPoisoning|CollectsDNSObservationsFromSliceData)' -count=1</automated>
  </verify>
  <acceptance_criteria>`internal/classifier/classifier.go` contains registry/orchestration scaffolding, `Classify(result)` delegates through normalized evidence instead of reading each layer inline, and DNS slice-backed observations still reach classification.</acceptance_criteria>
  <done>`Classify(result)` compiles with the new internal contracts and no public signature or package boundary changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock in registry behavior with deterministic regression tests</name>
  <objective>Add tests that prove the new foundation preserves visible behavior and deterministic detector sequencing before heuristics are extracted.</objective>
  <files>internal/classifier/classifier_test.go</files>
  <read_first>internal/classifier/classifier_test.go, internal/classifier/classifier.go, .planning/phases/04-dns-enhancements/04-04-SUMMARY.md</read_first>
  <behavior>
    - Existing DNS inconsistency and slice-data behavior still pass through `Classify`.
    - A composite target result yields findings in stable order that reflects the registry sequence.
  </behavior>
  <action>Extend `internal/classifier/classifier_test.go` with a composite `TargetResult` case that exercises more than one detector and asserts the returned finding order stays deterministic after registry introduction. Keep tests in external-package style (`package classifier_test`) and assert only through `classifier.Classify`, not unexported symbols. Reuse current inline struct-literal fixtures.</action>
  <verify>
    <automated>go test ./internal/classifier -count=1</automated>
  </verify>
  <acceptance_criteria>The classifier test suite includes an observable registry-order assertion and all existing classifier scenarios still pass through the new orchestration layer.</acceptance_criteria>
  <done>Later plans can safely move heuristics into detectors because the foundation has public-behavior regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProbeResult.Data -> classifier | Untrusted probe observations are converted into typed evidence for detectors. |
| detector registry -> findings | Internal detector sequencing controls which findings are emitted and in what order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | normalized evidence builder | mitigate | Normalize DNS single-item and slice payloads in one path and pin both shapes with classifier tests so findings cannot be dropped silently. |
| T-05-02 | T | detector registry ordering | mitigate | Keep an explicit ordered registry inside `internal/classifier` per D-02 and assert deterministic `Classify` output order through external-package regression tests. |
</threat_model>

<verification>
- `go test ./internal/classifier -count=1`
</verification>

<success_criteria>
- `Classify(result model.TargetResult) []model.Finding` remains unchanged.
- `internal/classifier` owns an ordered detector registry and one normalized evidence view.
- DNS slice-backed observations continue to classify correctly.
- The classifier suite proves deterministic observable ordering before detector extraction begins.
</success_criteria>

<output>
After completion, create `.planning/phases/05-classification-and-profile-improvements/05-01-SUMMARY.md`
</output>
