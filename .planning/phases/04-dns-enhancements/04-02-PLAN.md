---
phase: 04-dns-enhancements
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/probe/dnsprobe/dns.go
  - internal/probe/dnsprobe/doh.go
  - internal/probe/dnsprobe/dot.go
  - internal/probe/dnsprobe/dns_test.go
autonomous: true
requirements: [F-13, N-01]
must_haves:
  truths:
    - "DNS probe dispatches to correct transport based on Resolver.Transport"
    - "DoH queries send DNS wire-format via HTTP POST to https://server/dns-query"
    - "DoT queries use miekg/dns Client with Net:tcp-tls and InsecureSkipVerify"
    - "System resolver uses net.DefaultResolver.LookupHost"
  artifacts:
    - path: "internal/probe/dnsprobe/dns.go"
      provides: "Transport dispatcher in Probe(), udpQuery(), systemResolverQuery()"
    - path: "internal/probe/dnsprobe/doh.go"
      provides: "dohQuery() function with HTTP POST + DNS wire format"
    - path: "internal/probe/dnsprobe/dot.go"
      provides: "dotQuery() function with miekg/dns tcp-tls client"
  key_links:
    - from: "Probe() dispatcher in dns.go"
      to: "dohQuery() in doh.go"
      via: "case https switch branch"
    - from: "Probe() dispatcher in dns.go"
      to: "dotQuery() in dot.go"
      via: "case tcp-tls switch branch"
---

<objective>
Refactor the DNS probe to support multiple transports via a transport dispatcher, implement DoH (DNS over HTTPS via Go stdlib HTTP + miekg/dns wire format) and DoT (DNS over TLS via miekg/dns Net:"tcp-tls"), and add system resolver support via net.DefaultResolver.

Purpose: Enables encrypted DNS transport options (DoH, DoT) without new external dependencies — DoH uses Go stdlib HTTP, DoT uses the existing miekg/dns dependency. The transport dispatcher (switch on Resolver.Transport) is the architectural foundation for multi-resolver probing.
Output: Transport dispatcher in Probe(), udpQuery(), systemResolverQuery(), dohQuery() in doh.go, dotQuery() in dot.go, tests for each transport.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dns-enhancements/04-RESEARCH.md
@.planning/phases/04-dns-enhancements/04-PATTERNS.md

<interfaces>
Current Probe() function signature — MUST BE PRESERVED as the transport dispatcher:
```go
func Probe(ctx context.Context, resolver model.Resolver, domain string, qtype uint16, timeout time.Duration) model.DNSObservation
```

New Resolver.Transport field (from Plan 01):
```go
type Resolver struct {
    Name      string `json:"name"`
    Server    string `json:"server"`
    System    bool   `json:"system"`
    Transport string `json:"transport,omitempty"` // "", "udp", "tcp", "https", "tcp-tls", "system"
}
```

Existing patterns to follow:
- DNSObservation construction: create `model.DNSObservation{Resolver, Query, Type}` upfront
- EDNS0: `msg.SetEdns0(1232, false)` on every query
- Latency: `start := time.Now()` before exchange, `observation.Latency = time.Since(start)` after
- Error handling: `observation.Error = err.Error()` on failure, return observation
- Answer parsing: iterate `resp.Answer`, switch on `*mdns.A`, `*mdns.AAAA`, `*mdns.CNAME`
- RCODE: `observation.RCode = mdns.RcodeToString[resp.Rcode]`
- Success: `observation.Success = resp.Rcode == mdns.RcodeSuccess`

CRITICAL CORRECTIONS from RESEARCH.md:
- D-03 corrected: miekg/dns does NOT support `Net: "https"`. DoH MUST use Go `net/http` POST with DNS wire-format (`application/dns-message` content type).
- D-06 corrected: Passing empty server string to miekg/dns does NOT use system resolver. Use `net.DefaultResolver.LookupHost(ctx, domain)` instead.
- DoT port default: 853, NOT 53 (Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor dns.go — extract udpQuery, add transport dispatcher, add systemResolverQuery</name>
  <files>internal/probe/dnsprobe/dns.go</files>
  <read_first>internal/probe/dnsprobe/dns.go</read_first>
  <action>
Refactor `internal/probe/dnsprobe/dns.go` with four changes:

1. Rename the current `Probe()` function to `udpQuery()`. Keep the entire body intact — UDP client, TCP truncated fallback, answer parsing, everything. Do NOT change the function signature or the logic. This is a pure rename + export (keep unexported: lowercase `udpQuery`).

2. Add a `systemResolverQuery()` function that uses Go's `net.DefaultResolver`:
   ```go
   func systemResolverQuery(ctx context.Context, resolver model.Resolver, domain string, qtype uint16, timeout time.Duration) model.DNSObservation {
       obs := model.DNSObservation{Resolver: resolver.Name, Query: domain, Type: mdns.TypeToString[qtype]}
       start := time.Now()
       addrs, err := net.DefaultResolver.LookupHost(ctx, domain)
       obs.Latency = time.Since(start)
       if err != nil {
           var dnsErr *net.DNSError
           if errors.As(err, &dnsErr) {
               if dnsErr.IsNotFound {
                   obs.RCode = "NXDOMAIN"
               } else if dnsErr.IsTemporary {
                   obs.RCode = "SERVFAIL"
               }
           }
           obs.Error = err.Error()
           return obs
       }
       obs.RCode = "NOERROR"
       obs.Success = true
       obs.Answers = addrs
       return obs
   }
   ```
   This function is called when `resolver.Transport == "system"`.

3. Create a new `Probe()` function as a transport dispatcher:
   ```go
   func Probe(ctx context.Context, resolver model.Resolver, domain string, qtype uint16, timeout time.Duration) model.DNSObservation {
       switch resolver.Transport {
       case "https":
           return dohQuery(ctx, resolver, domain, qtype, timeout)
       case "tcp-tls":
           return dotQuery(ctx, resolver, domain, qtype, timeout)
       case "system":
           return systemResolverQuery(ctx, resolver, domain, qtype, timeout)
       default: // "", "udp", "tcp"
           return udpQuery(ctx, resolver, domain, qtype, timeout)
       }
   }
   ```
   The default case handles `""` (backward compatible) and `"udp"` and `"tcp"`.

4. Add `"errors"` to imports (needed for `errors.As` in systemResolverQuery).

Do NOT use the name `eds0` anywhere. Do NOT change the existing `missingPort` function. Do NOT remove the `mdns` import alias.
  </action>
  <verify>
    <automated>go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'func udpQuery' internal/probe/dnsprobe/dns.go → exactly 1
    - grep -c 'func systemResolverQuery' internal/probe/dnsprobe/dns.go → exactly 1
    - grep -c 'func Probe' internal/probe/dnsprobe/dns.go → exactly 1
    - grep -c 'case "https":' internal/probe/dnsprobe/dns.go → exactly 1
    - grep -c 'case "tcp-tls":' internal/probe/dnsprobe/dns.go → exactly 1
    - grep -c 'case "system":' internal/probe/dnsprobe/dns.go → exactly 1
    - grep -c 'default:' internal/probe/dnsprobe/dns.go → exactly 1
    - grep -c 'DefaultResolver.LookupHost' internal/probe/dnsprobe/dns.go → exactly 1
    - grep -c '"errors"' internal/probe/dnsprobe/dns.go → exactly 1
    - go build ./... succeeds
    - go test ./internal/probe/dnsprobe/... passes
  </acceptance_criteria>
  <done>udpQuery extracted, Probe() is a transport dispatcher, systemResolverQuery exists. Build and dnsprobe tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create doh.go with dohQuery implementation + doh_test.go</name>
  <files>internal/probe/dnsprobe/doh.go, internal/probe/dnsprobe/doh_test.go</files>
  <read_first>internal/probe/dnsprobe/dns.go, internal/probe/dnsprobe/dns_test.go, internal/probe/httpprobe/http.go</read_first>
  <action>
Create `internal/probe/dnsprobe/doh.go`:

1. Package `dnsprobe`. Imports: `"bytes"`, `"context"`, `"io"`, `"net/http"`, `"time"`, plus `mdns "github.com/miekg/dns"` and `"iscan/internal/model"`.

2. `dohQuery(ctx context.Context, resolver model.Resolver, domain string, qtype uint16, timeout time.Duration) model.DNSObservation` per the RESEARCH.md Pattern 2:
   - Construct DNSObservation with `Resolver: resolver.Name`, `Query: mdns.Fqdn(domain)`, `Type: mdns.TypeToString[qtype]`
   - Create msg with `SetQuestion(query, qtype)` and `SetEdns0(1232, false)`
   - Pack with `msg.Pack()`
   - Build DoH URL: `https://{resolver.Server}/dns-query` (resolver.Server already contains the host)
   - Create HTTP request with `http.NewRequestWithContext(ctx, http.MethodPost, dohURL, bytes.NewReader(packed))`
   - Set headers: `Content-Type: application/dns-message`, `Accept: application/dns-message`
   - Execute with `&http.Client{Timeout: timeout}`
   - Read body with `io.ReadAll`
   - Unpack response with `new(mdns.Msg)` → `dnsResp.Unpack(body)`
   - Set `obs.RCode = mdns.RcodeToString[dnsResp.Rcode]`
   - Set `obs.Success = dnsResp.Rcode == mdns.RcodeSuccess`
   - Parse answers using the same loop as in udpQuery (A, AAAA, CNAME)
   - Record latency from `time.Now()` before HTTP POST
   - Error handling: `obs.Error = "doh: " + err.Error()` prefix on failures

3. The function signature is `dohQuery` (unexported, called from Probe() dispatcher).

Create `internal/probe/dnsprobe/doh_test.go`:

1. Package `dnsprobe_test` (external test package per codebase convention).
2. Import `context`, `testing`, `time`, `mdns`, `model`, `dnsprobe` plus `crypto/tls`, `net`, `net/http`, `net/http/httptest`.
3. `TestProbeDoH(t *testing.T)`:
   - Use `httptest.NewTLSServer` with a handler that:
     - Checks `r.Header.Get("content-type") == "application/dns-message"`
     - Reads body bytes
     - Unpacks into `mdns.Msg` with `Unpack(body)`
     - Builds reply: `SetReply(unpacked)`, adds A record for `203.0.113.10` with TTL 60
     - Packs reply and writes with `w.Header().Set("content-type", "application/dns-message")`
   - Extract host from server URL (strip `https://` prefix)
   - Call `dnsprobe.Probe(ctx, Resolver{Name: "local", Server: host, Transport: "https"}, "example.com", mdns.TypeA, 2*time.Second)`
   - Assert: `observation.Success == true`
   - Assert: `observation.RCode == "NOERROR"`
   - Assert: `len(observation.Answers) == 1 && observation.Answers[0] == "203.0.113.10"`
   - Register `server.Close()` with `t.Cleanup`
4. `TestProbeDoHError(t *testing.T)`:
   - Call `dnsprobe.Probe(ctx, Resolver{Name: "bad", Server: "127.0.0.1:1", Transport: "https"}, "example.com", mdns.TypeA, 100*time.Millisecond)`
   - Assert: `observation.Success == false`
   - Assert: `observation.Error != "" && strings.Contains(observation.Error, "doh:")`
5. Use `string` import for `strings.Contains`.
  </action>
  <verify>
    <automated>go test ./internal/probe/dnsprobe/... -run TestProbeDoH -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'func dohQuery' internal/probe/dnsprobe/doh.go → exactly 1
    - grep -c 'application/dns-message' internal/probe/dnsprobe/doh.go → exactly 1
    - grep -c 'http.NewRequestWithContext' internal/probe/dnsprobe/doh.go → exactly 1
    - grep -c 'msg.Pack()' internal/probe/dnsprobe/doh.go → exactly 1
    - grep -c 'dnsResp.Unpack' internal/probe/dnsprobe/doh.go → exactly 1
    - grep -c 'func TestProbeDoH' internal/probe/dnsprobe/doh_test.go → exactly 1
    - grep -c 'func TestProbeDoHError' internal/probe/dnsprobe/doh_test.go → exactly 1
    - grep -c 'Transport.*"https"' internal/probe/dnsprobe/doh_test.go → exactly 1
    - go build ./... succeeds
    - go test ./internal/probe/dnsprobe/... -run TestProbeDoH -count=1 passes
  </acceptance_criteria>
  <done>doh.go implements DoH via HTTP POST with DNS wire format. doh_test.go verifies with local HTTPS test server. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create dot.go with dotQuery implementation + dot_test.go</name>
  <files>internal/probe/dnsprobe/dot.go, internal/probe/dnsprobe/dot_test.go</files>
  <read_first>internal/probe/dnsprobe/dns.go, internal/probe/dnsprobe/dns_test.go, internal/probe/tlsprobe/tls.go</read_first>
  <action>
Create `internal/probe/dnsprobe/dot.go`:

1. Package `dnsprobe`. Imports: `"context"`, `"crypto/tls"`, `"net"`, `"time"`, plus `mdns "github.com/miekg/dns"` and `"iscan/internal/model"`.

2. `dotQuery(ctx context.Context, resolver model.Resolver, domain string, qtype uint16, timeout time.Duration) model.DNSObservation` per the RESEARCH.md Pattern 3:
   - Construct DNSObservation same pattern as udpQuery and dohQuery
   - Create msg with `SetQuestion(query, qtype)` and `SetEdns0(1232, false)`
   - Determine server address: `server := resolver.Server`. If `missingPort(server)` is true, append `:853` (DoT default port, NOT 53 — per RESEARCH Pitfall 4). `missingPort()` already exists in dns.go.
   - Create client: `&mdns.Client{Net: "tcp-tls", Timeout: timeout, TLSConfig: &tls.Config{InsecureSkipVerify: true}}`
   - Call `client.ExchangeContext(ctx, msg, server)`
   - Set `obs.RCode = mdns.RcodeToString[resp.Rcode]`
   - Set `obs.Success = resp.Rcode == mdns.RcodeSuccess`
   - Parse answers with the same A/AAAA/CNAME loop as udpQuery
   - Handle truncated responses: if `resp.Truncated`, do a second `ExchangeContext` with the same client (TCP can handle large responses). On retry failure, set `obs.Error = "dot_truncated_retry_failed: " + err.Error()`
   - Record latency from `time.Now()` before ExchangeContext
   - Error prefix: `obs.Error = "dot: " + err.Error()`

3. `dotQuery` is unexported (called from Probe() dispatcher).

Create `internal/probe/dnsprobe/dot_test.go`:

1. Package `dnsprobe_test`. Imports: `context`, `crypto/tls`, `net`, `strings`, `testing`, `time`, plus `mdns`, `model`, `dnsprobe`.
2. `startDoTServer(t *testing.T) string` helper:
   - Create a `mdns.ServeMux` with handler that replies with A record `203.0.113.10` for TypeA queries
   - Generate a self-signed cert with `tls.X509KeyPair` — use hardcoded test cert/key (any PEM cert/key pair works since InsecureSkipVerify is true)
   - Create `tls.Config{Certificates: []tls.Certificate{cert}}`
   - Listen on `127.0.0.1:0` with `tls.Listen("tcp", addr, tlsConfig)`
   - Start `mdns.Server{Listener: listener, Handler: mux}` in goroutine
   - Register `t.Cleanup(server.Shutdown)`
   - Return `listener.Addr().String()`
3. `TestProbeDoT(t *testing.T)`:
   - Call helper to start server
   - Call `dnsprobe.Probe(ctx, Resolver{Name: "local", Server: addr, Transport: "tcp-tls"}, "example.com", mdns.TypeA, 2*time.Second)`
   - Assert: `observation.Success == true`
   - Assert: `observation.RCode == "NOERROR"`
   - Assert: `len(observation.Answers) == 1 && observation.Answers[0] == "203.0.113.10"`
4. `TestProbeDoTDefaultPort(t *testing.T)`:
   - Call `dnsprobe.Probe` with `Server: "127.0.0.1"` (no port, Transport: "tcp-tls") → should fail since port 853 not running
   - Assert: `observation.Success == false`
   - Assert: `observation.Error != "" && strings.Contains(observation.Error, "dot:")`
  </action>
  <verify>
    <automated>go test ./internal/probe/dnsprobe/... -run TestProbeDoT -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'func dotQuery' internal/probe/dnsprobe/dot.go → exactly 1
    - grep -c 'Net:.*"tcp-tls"' internal/probe/dnsprobe/dot.go → exactly 1
    - grep -c 'InsecureSkipVerify.*true' internal/probe/dnsprobe/dot.go → exactly 1
    - grep -c ':853' internal/probe/dnsprobe/dot.go → exactly 1 (DoT default port)
    - grep -c 'func TestProbeDoT' internal/probe/dnsprobe/dot_test.go → exactly 1
    - grep -c 'Transport.*"tcp-tls"' internal/probe/dnsprobe/dot_test.go → at least 1
    - grep -c 'startDoTServer' internal/probe/dnsprobe/dot_test.go → exactly 1
    - go build ./... succeeds
    - go test ./internal/probe/dnsprobe/... -run TestProbeDoT -count=1 passes
  </acceptance_criteria>
  <done>dot.go implements DoT via miekg/dns Client{Net:"tcp-tls"}. dot_test.go verifies with local TLS server. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DoH HTTPS POST → external resolver | DNS wire-format messages sent over HTTPS to user-configured DoH endpoint |
| DoT TLS → external resolver | DNS messages over TLS to user-configured DoT endpoint |
| System resolver → OS | System DNS queries via Go stdlib |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Spoofing | dot.go DoT TLS | mitigate | `InsecureSkipVerify: true` by design (diagnostic tool per N-06). Documented — consistent with all other probes. |
| T-04-03 | Tampering | doh.go HTTP response | mitigate | DNS response unpacked via miekg/dns `Unpack()` which validates wire format. Malformed responses produce errors. |
| T-04-04 | Denial of Service | doh.go/dot.go network calls | mitigate | All network calls bound by `ctx` deadline and per-resolver `timeout`. No unbounded operations. |
</threat_model>

<verification>
- `go build ./...` succeeds
- `go test ./internal/probe/dnsprobe/... -count=1` passes all tests (existing + new DoH/DoT tests)
- `go vet ./internal/probe/dnsprobe/...` passes
</verification>

<success_criteria>
1. `Probe()` is a transport dispatcher switching on `resolver.Transport` with cases for `"https"`, `"tcp-tls"`, `"system"`, and default (udp)
2. Existing UDP/TCP behavior preserved as `udpQuery()` — all existing DNS tests pass unchanged
3. `dohQuery()` in `doh.go` sends DNS wire-format via HTTP POST to `https://{server}/dns-query`
4. `dotQuery()` in `dot.go` uses `miekg/dns.Client{Net: "tcp-tls"}` with `InsecureSkipVerify: true`, defaults to port 853
5. `systemResolverQuery()` uses `net.DefaultResolver.LookupHost` and maps `net.DNSError` to RCODEs
6. DoH and DoT tests verify correct observation output against local test servers
7. No new external dependencies (DoH uses stdlib `net/http`, DoT uses existing `miekg/dns`)
</success_criteria>

<output>
After completion, create `.planning/phases/04-dns-enhancements/04-02-SUMMARY.md`
</output>
